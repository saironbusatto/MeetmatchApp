# Wiki ingest evidence — Mobile one-shot completion

## What changed
- Finalizadas tasks mobile #22–#32 no Taskmaster (status `done`).
- API: adicionado endpoint `POST /users/me/devices` para registrar token push por usuário autenticado.
- API/store: adicionado storage de devices (`userDevices`) e `upsertUserDevice`.
- Mobile push:
  - `lib/push.ts` agora registra device no backend (`registerDeviceForPush`).
  - onboarding/signup integrados com solicitação de permissão push e registro de token.
  - `app/_layout.tsx` adicionou handler de resposta de notificação com deep link (`targetPath`).
- Qualidade mobile:
  - Novos componentes de estado (`EmptyState`, `LoadingState`, `ErrorState`).
  - Melhorias de a11y com `accessibilityLabel` em CTAs críticas.
  - Fluxos Maestro adicionados em `apps/mobile/.maestro/`.
- CI/CD mobile:
  - `apps/mobile/eas.json` com perfis development/preview/production e canal `stable`.
  - `.github/workflows/mobile-release.yml` para tags `mobile-*`.
  - `app.json` com `runtimeVersion` e `updates.url` para OTA.
- Documentação:
  - `apps/mobile/README.md` ampliado com Maestro, push e release.
  - README raiz com seção "Mobile app (implemented)".
  - `CLAUDE.md` atualizado para refletir stack adotado.

## Why it changed
- Encerrar o backlog mobile em modo one-shot, com base funcional para desenvolvimento contínuo e pipeline de release.

## Validation evidence
- `pnpm --filter @farmei/api typecheck` ✅
- `pnpm --filter @farmei/mobile typecheck` ✅
- `pnpm --filter @farmei/mobile dev -- --help` ✅
- Verificação de tasks mobile: `22..32 = done` ✅
