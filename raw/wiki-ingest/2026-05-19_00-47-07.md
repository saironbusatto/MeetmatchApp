# Ingest — Security audit Credenciais.md

Data: 2026-05-19

## O que foi verificado
- Conteúdo de `Credenciais.md`.
- Status de versionamento do arquivo no git.
- Regras de ignore em `.gitignore`.
- Histórico completo (`--all`) para `Credenciais.md`, `*.key`, `*.key.pub` e `.env`.

## Resultado
- `Credenciais.md` contém IP público e usuário SSH; não contém chave privada, senha, token ou secret.
- `Credenciais.md` está ignorado por `.gitignore` (linha 30).
- Não há evidência de commit prévio de `Credenciais.md` no histórico local (`git rev-list --all -- Credenciais.md` vazio).
- Não há evidência de commit prévio de `*.key`, `*.key.pub` ou `.env` no histórico local.

## Evidências (comandos)
- `git check-ignore -v Credenciais.md`
- `git rev-list --all -- Credenciais.md`
- `git rev-list --all -- '*.key' '*.key.pub' '.env'`
- `nl -ba .gitignore | sed -n '1,120p'`
- `git remote -v`

