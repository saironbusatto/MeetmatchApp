# Handoff: Farmei — redesign OD (branch `redesign/od-prototype`)

**Data:** 2026-09-07
**Réplica saindo:** @saironcb

---

## Ativos

Nenhum incidente aberto. Working tree **limpo** após commit `81cc4c5`.

## Investigações em andamento

Nenhuma investigação ativa — os 3 passos do usuário foram concluídos e verificados.

## Resolvido nesta sessão

1. **Janela de confirmação 24h + re-match (private-events)** — `confirmationWindowEndsAt` ~24h, quórum via date-suggestion V2, re-match no `dia-do-bolo leave` (key detona re-match; demais só se não puder cumprir; `NO_DATE` sem candidato; migração de estado ao sair para permitir re-join).
2. **Waitlist (public-events)** — capacidade cheia vira `WAITLIST` com `position` FIFO; cancelar `REGISTERED` promove o primeiro da fila; `GET /:id/waitlist` owner-only; CSV com `position`; `admissionMode` `FIRST_COME`/`CONFIAVEL`.
3. **Migração `0002_puzzling_umar`** — 16 statements (enums `NO_DATE`, `WAITLIST`, `ROLE_ALEATORIO`, `admission_mode`; colunas `slot`, `confirmed_slot`, `confirmation_window_ends_at`, `position`, `quorum_min`, `indicated_by`, `invite_status_activated_at`; índices únicos; drop drift `key_person_weight`). `drizzle-kit check` OK.
4. **Correção de journal** — `0001_snapshot.json` jamais commitado foi reconstruído e commitado.

## Mudanças recentes

| Item | Estado |
|---|---|
| Commit `81cc4c5` (13 arquivos, +2012/−30) | branch `redesign/od-prototype` |
| Testes API (vitest) | 16/16 OK (`__tests__/helpers.ts`, `private-events.confirmation`, `public-events.waitlist`) |
| Testes utils | 22/22 OK |
| Build API / `packages/types` | OK |

## Issues conhecidas / ressaltos

- `ALTER TYPE ... ADD VALUE` fora de transação (breakpoint do drizzle) — limitação do Postgres, não bloqueia.
- **Lint do monorepo pré-quebrado** (ESLint 9 vs `.eslintrc.*`) — fora de escopo, já existia.
- `0001_snapshot.json` reconstruído (nunca esteve no git antes) — validado por `drizzle-kit check`, mas sem claim de fidelidade byte-a-byte ao banco real.
- Regras wiki D17/D18/D27/D28/D30 guiaram as implementações.

## Próximos eventos

- **Andar da fila de trabalho:** 3 passos do usuário concluídos. Nada pendente nesta sessão.
- **Watch:** aplicar o `0002` num PG real (rodar `drizzle-kit migrate` / `push`) e conferir o resultado; revisar drift do `key_person_weight` com o gerador de migração.

## Escalonamento

| Tipo | Primeiro | Segundo |
|---|---|---|
| Migração/DB | @user (dono da spec) | Wiki (D17–D30) |
| Lint/escala monorepo | fora de escopo da sessão | — |

## Checklist do ingressante

- [ ] `git status` na `redesign/od-prototype` (limpo)
- [ ] Rodar `pnpm --filter @farmei/api run test` e `build`
- [ ] Decidir bot de migração real vs `drizzle-kit push`